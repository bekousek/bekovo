---
interface BreadcrumbItem {
  label: string;
  href?: string;
}

interface Props {
  items: BreadcrumbItem[];
  /** Kořen drobečkové navigace. Fyzika má „Témata“ → „/“, OSV „OSV“ → „/osv“. */
  rootLabel?: string;
  rootHref?: string;
}

const { items, rootLabel = 'Témata', rootHref = '/' } = Astro.props;
---
<nav class="mb-6 text-sm text-gray-500" aria-label="Breadcrumb">
  <ol class="flex items-center gap-1.5 flex-wrap">
    <li>
      <a href={rootHref} class="hover:text-primary-600 transition-colors">{rootLabel}</a>
    </li>
    {items.map((item) => (
      <li class="flex items-center gap-1.5">
        <span class="text-gray-300">/</span>
        {item.href ? (
          <a href={item.href} class="hover:text-primary-600 transition-colors">{item.label}</a>
        ) : (
          <span class="text-gray-900 font-medium">{item.label}</span>
        )}
      </li>
    ))}
  </ol>
</nav>
